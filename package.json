{
  "name": "studio-visual-crm-integrations",
  "version": "1.0.0",
  "description": "Backend de integrações (OAuth + Webhooks) para o Studio Visual CRM AI",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
